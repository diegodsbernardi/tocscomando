name: SAIPOS mix mensal

concurrency:
  group: saipos-sessao
  cancel-in-progress: false

on:
  workflow_dispatch:
    inputs:
      inicio:
        description: "Mês inicial YYYY-MM (default 2025-06)"
        type: string
        required: false
      fim:
        description: "Mês final YYYY-MM (default mês corrente)"
        type: string
        required: false

jobs:
  mix:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - run: npm install --no-audit --no-fund
      - run: npx playwright install --with-deps chromium
      - name: Extrair mix mensal
        env:
          SAIPOS_USER: ${{ secrets.SAIPOS_USER }}
          SAIPOS_PASS: ${{ secrets.SAIPOS_PASS }}
          SAIPOS_BASE_URL: ${{ secrets.SAIPOS_BASE_URL }}
          SAIPOS_STORE_IDS: ${{ secrets.SAIPOS_STORE_IDS }}
          MIX_INICIO: ${{ inputs.inicio }}
          MIX_FIM: ${{ inputs.fim }}
        run: node scripts/itens_mensais.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mix-mensal
          path: mix-mensal.csv
          if-no-files-found: warn
